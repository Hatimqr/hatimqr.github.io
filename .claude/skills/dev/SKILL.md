---
name: dev
description: Start the Vite dev server to preview the website locally
disable-model-invocation: true
allowed-tools: Bash
---

Start the dev server so the user can check how their website looks.

Run the following command in the background:

```
npm run dev
```

After starting, tell the user the local URL (usually http://localhost:5173) and remind them they can stop it with Ctrl+C in the terminal or by asking you to stop it.
